'use strict';

var expect = require("expect.js");
var star_theme = require("../lib/star-theme");


describe("star_theme", function () {
    it("is defined", function () {
        expect(star_theme).to.be.an('object');
    });
});
